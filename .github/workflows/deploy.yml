name: 🚀 Deploy to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout code
        uses: actions/checkout@v4

      - name: 🔑 Setup SSH and Deploy
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 13.61.12.156
          username: ubuntu
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          script: |
            cd /var/www/html/mini-crm
            git pull origin main
            npm install --production
            pm2 restart all
